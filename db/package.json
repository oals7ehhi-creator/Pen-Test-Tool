{
  "name": "@pentest/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "migrate": "node dist/migrate.js up",
    "migrate:down": "node dist/migrate.js down",
    "migrate:ci": "node dist/migrate.js ci"
  },
  "dependencies": {
    "@pentest/shared": "workspace:*",
    "pg": "8.13.1"
  },
  "devDependencies": {
    "@types/pg": "8.11.10"
  }
}
